package thkoeln.dungeon.command.application;

import org.springframework.stereotype.Service;

// TODO - this is work in progress, not much use for your own player as of now.

@Service
public class CommandApplicationService {

    /**
     * This method can be called the GameService roundStatus event listener: It then
     * collects all commands to be sent to the GameService.
     */
    public void sendAllCommandsForOneRound() {

    }
}
